version: '3'

services:  
  web:
    build: "."
    restart: always
    command: python -u code.py                
    external_links:
      - postgres
    networks:
      - mynet

networks:
  mynet:
    external:
      name: mynet      
    # restart: "unless-stopped"          
